# Phase 6 · 模块二:更丰富的终端界面(TUI)实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交互模式下用 ratatui + crossterm 提供多区域布局(对话主输出区、待办面板、权限确认弹层含 diff 预览、输入盒、状态行);渲染循环与 agent 事件流经 channel 解耦;`--plain` 与非 TTY 自动降级为现有纯文本流模式。

**Architecture:** agent 循环留在 tokio 侧,渲染跑在独立同步线程,双 channel 解耦:agent → UI 用 `std::sync::mpsc::Sender<UiEvent>`(非阻塞 send,UI 线程按 ~50Hz 排空重绘);UI → agent 用 `tokio::sync::mpsc`(`blocking_send` 自同步线程安全),Ctrl+C 打断走独立 interrupt channel 配合 `tokio::select!`。权限确认由 `TuiConfirm`(`PermissionHandler` 实现)经 `oneshot` 把裁决权交给 UI 线程的确认弹层。纯文本模式(`--plain` / 非 TTY / 单任务模式)完全复用现有代码路径。本计划依赖 Phase 6 模块一计划(subagent)已合入——`build_loop` 已改为注入 `handler`/`todos`,本计划在此之上只改渲染层。

**Tech Stack:** ratatui 0.28(与现有 crossterm 0.28 对齐)、现有 tokio/anyhow/anstream。

## Global Constraints(摘自 AGENTS.md,每个任务隐含遵守)

- UI 硬约束:**所有颜色/ANSI 只从 `ui/theme.rs` 取**,其他文件禁止裸 `\x1b[`;TUI 侧颜色用 theme.rs 里的 `ratatui::style::Color` 常量(本计划 Task 2 添加)。
- raw mode 期间换行必须显式 `\r\n`(现有纯文本路径约束;TUI 用 ratatui 自绘不受影响,但降级路径必须保持)。
- 非测试代码禁止裸 `unwrap()`/`expect()`(Mutex 用 `unwrap_or_else(|p| p.into_inner())`,Option/Result 用显式处理)。
- `lex-cli → lex-core` 单向依赖;TUI 不得绕过 `execute_tool_call` 权限路径。
- 提示词/交互输出用中文。
- 构建:先 `export PATH="$HOME/.cargo/bin:/d/mingw64/bin:$PATH"`,再 `cargo test --workspace`。
- 提交信息用中文 conventional commits。

---

## 开工前已知偏差(2026-09-17 核对,先读本节再动手)

> 对全文约 55 处「关于现存代码」的断言逐条比对真实仓库:约 33 处一致,**19 处不符,其中 10 处阻塞**。本节优先于下文正文。
>
> **行号约定**:本节内引用的行号以**插入本节之前的原文**为准(本节插入使其后正文行号整体 **+53**)。每条均附原文摘录,行号对不上时按摘录检索。

### ⚠️ 本计划的最大前提是假的

架构段(L7)、L932、L1189 三处把「模块一(subagent)Task 7 已合入、`build_loop` 已注入 handler/todos」当作既成事实,其中 L1189 直接写「该计划已完成」。

**实际完全未合入**:

- `build_loop` 真实签名仍是 `build_loop(cfg, cwd: PathBuf, input: Arc<CliInput>, renderer: &Arc<Mutex<Renderer>>)`(`lex-cli/src/main.rs:73-78`)
- `run()` 里**没有** `todos` 变量(`main.rs:186-188`)
- `tool_ctx.todos` 在 `build_loop` 内部就地 `Default::default()`,**外部拿不到句柄**(`main.rs:127`)

**后果**:Task 6/7 的全部接线建立在这条共享链上,而**本计划没有任何 Task 负责建立「`run()` 层的 todos Arc == `ToolContext.todos`」**。更危险的是它表现为**静默失败**——两处若不是同一个 `Arc`,待办面板永远空白,而 Task 5 的测试直接传自己造的 Mutex、不走真实装配,因此**单测发现不了**,只能靠手工验收暴露。

**处理**:本计划须在模块一合并后**重新核对 `main.rs` 现状**再开工;并在 Task 6/7 增加一条覆盖该链的断言(例如给 `ToolContext.todos` 预置数据后确认面板能渲染出来)。

### 阻塞项(照抄即编译不过)

| 位置 | 问题 | 修法 |
|---|---|---|
| L62 / L570 | `ToolContext { …, spawner: None }`——该字段是模块一 Task 3 才新增的 | 依赖模块一先行 |
| L62-63 | `let ctx = ToolContext{…};` 遮蔽同模块辅助函数 `ctx()`(`lex-core/src/security/mod.rs:190`),`&ctx()` 变成对值取引用 → E0618 | 改名如 `tc` |
| L69-75 | `Capture<'a>(&'a mut Option<_>)` + `async fn confirm(&self)` 无法经共享引用写穿 `&'a mut` → E0596 | 改用 `Arc<Mutex<Option<_>>>` 或 channel |
| L364 / L384 | `use anyhow::Result` + `-> Result<bool>`,但 trait 要求 `lex_core::error::Result`(LexError)→ E0053 | 照 `lex-cli/src/confirm.rs:2-3` 用 `lex_core::error::Result` |
| L410 vs L425 | 测试用 `{o:?}` 打印 `UiEvent`,L425 又规定该枚举不加 derive → E0277 | 二选一:加 `#[derive(Debug)]` 或改断言 |
| L403-413 | `#[tokio::test]` 默认 current_thread,主任务同步阻塞 `rx.recv()` 时 spawn 的任务永不被 poll → **测试永久挂起**(cargo test 不返回) | 加 `#[tokio::test(flavor = "multi_thread")]` |
| L500-501 | `draw.rs` 的 `mod tests` 内 `use super::state::…`——`super` 是 `tui::draw`,`tui::draw::state` 不存在 → E0432 | 改 `use crate::tui::state::…` |
| L762-768 vs L567-574 | `KeyOutcome` 无 derive 却被 `assert_eq!` 比较 | 加 `#[derive(Debug, PartialEq)]` |
| L567 | 测试用 `KeyCode`,但测试模块与 draw.rs 顶部导入清单均无此名 → E0433 | 补 `use crossterm::event::KeyCode;` |
| L7/L932/L1189 | 见上「最大前提是假的」 | 模块一合并后重新核对 |

### 需修订

- **L492** Interfaces 声明了 `push_line`(实现里没有);实现里有测试依赖的 `open_confirm`(L699)却没写进 Interfaces。
- **L207** 说「上限 400 行、截断文案 `…(截断)`」;实现(L224、L258-261)是 401 行、文案 `…(diff 已截断)`。
- **Task 3 Step 1**(L210-313)已把 `line_diff` 完整实现写进「写失败测试」步骤 → Step 2 不可能看到红。且 `main.rs` 未声明 `mod tui;` 时 `cargo test -p lex-cli tui::diff` 只会报 0 个匹配(退出码 0),不是 FAIL。
- **L927-929** Files 清单漏了 `lex-cli/src/main.rs`(L1106 要把 `detect_project_type` 改 `pub(crate)`,L1116 的 `git add` 又包含它)。
- **L1208 / L1225** 要求改 `AGENTS.md`——该文件已不存在(同模块一 §6)。
- **L1177** `use_tui(cli.plain, std::io::stdout().is_tty())` 只判 stdout;既有两条降级路径都是 `stdin().is_tty() && stdout().is_tty()`(`ui/input.rs:205`、`ui/settings.rs:310`)。只判 stdout 会让 `lex-code < file` 这类场景进 TUI。
- **L1248**「`--plain` **保留**」措辞易读作已存在;`--plain` 由本计划 Task 7 新增,现 `Cli` 只有 `task` / `cwd`(`main.rs:29-35`)。
- **L1226** 的「第 11 节」依赖模块一先写入「第 10 节」(`examples/smoke/README.md` 现末节为第 9 节)。
- **L33** 称 `pub fn detail_of`,实现(L99)是私有,L131 自注承认。
- **`interactive_session` 最终签名两份计划都没给**:模块一只有一句「签名随之适配」,本计划 L1189 按旧签名假定——**开工前需确认**。

### 基线

- 测试总数实为 **140**(与 CLAUDE.md 一致;模块一计划里写的 111 是旧数)。本次核对时基线全绿。
- 其中 `lex-cli` 的 `ui::settings` 测试原有一处 ~10% 概率的并行竞态(共享固定临时目录),**已于 2026-09-17 修复**,现 200 轮压测 0 失败。

---

### Task 1: core 侧 PendingAction 增加结构化 `detail`(TUI 弹层渲染 diff 的数据源)

**Files:**
- Modify: `lex-core/src/security/mod.rs`(`PendingAction`、`execute_tool_call`)
- Test: `lex-core/src/security/mod.rs`(`mod tests`)

**Interfaces:**
- Produces:
  - `#[derive(Debug, Clone, PartialEq)] pub enum PendingDetail { Bash { command: String }, FileEdit { path: String, old_string: String, new_string: String }, Other }`
  - `PendingAction` 新增 `pub detail: PendingDetail`(summary 字段保留,纯文本模式不受影响)。
  - `pub fn detail_of(tool_name: &str, input: &Value) -> PendingDetail`
- 消费方:Task 5(UI 确认弹层)、现有 `CliInput`(只读 summary,不受影响)。

- [ ] **Step 1: 写失败测试**

`lex-core/src/security/mod.rs` 的 `mod tests` 追加:

```rust
    #[test]
    fn detail_of_extracts_bash_and_file_edit() {
        let d = detail_of("bash_exec", &serde_json::json!({"command":"cargo test"}));
        assert_eq!(d, PendingDetail::Bash { command: "cargo test".into() });

        let d = detail_of("file_edit", &serde_json::json!({"path":"a.rs","old_string":"x","new_string":"y"}));
        assert_eq!(
            d,
            PendingDetail::FileEdit { path: "a.rs".into(), old_string: "x".into(), new_string: "y".into() }
        );

        assert_eq!(detail_of("grep_search", &serde_json::json!({"pattern":"p"})), PendingDetail::Other);
        assert_eq!(detail_of("todo_write", &serde_json::json!({})), PendingDetail::Other);
    }

    #[tokio::test]
    async fn confirm_action_carries_detail() {
        let mut reg = ToolRegistry::new();
        reg.register(Box::new(Echo)); // 非只读 → Confirm 路径
        let mut action: Option<PendingAction> = None;
        let handler = Capture(&mut action);
        let ctx = ToolContext { cwd: PathBuf::from("."), shell: None, todos: Default::default(), spawner: None };
        let _ = execute_tool_call(&reg, &handler, &ctx, &guard(), "t9", "echo", serde_json::json!({"x":"1"})).await;
        let a = action.unwrap_or_else(|| panic!("应捕获到 PendingAction"));
        assert_eq!(a.detail, PendingDetail::Other);
    }

    // 捕获型 handler(测试辅助)
    struct Capture<'a>(&'a mut Option<PendingAction>);
    #[async_trait::async_trait]
    impl PermissionHandler for Capture<'_> {
        async fn confirm(&self, a: &PendingAction) -> crate::error::Result<bool> {
            *self.0 = Some(a.clone());
            Ok(true)
        }
    }
```

注:现有测试内 `Echo`/`ctx()`/`guard()` 已存在,直接复用;`PendingAction` 构造点在本任务内同步补 `detail` 字段,现有测试若字面量构造 `PendingAction` 需补 `detail: PendingDetail::Other`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p lex-core security::tests::detail_of`
Expected: FAIL,`PendingDetail` 不存在。

- [ ] **Step 3: 最小实现**

`lex-core/src/security/mod.rs`:

```rust
/// 结构化动作明细:TUI 权限弹层渲染 diff/命令原文用;纯文本模式只读 summary。
#[derive(Debug, Clone, PartialEq)]
pub enum PendingDetail {
    Bash { command: String },
    FileEdit { path: String, old_string: String, new_string: String },
    Other,
}

fn detail_of(tool_name: &str, input: &Value) -> PendingDetail {
    match tool_name {
        "bash_exec" => PendingDetail::Bash {
            command: input.get("command").and_then(Value::as_str).unwrap_or("").to_string(),
        },
        "file_edit" => PendingDetail::FileEdit {
            path: input.get("path").and_then(Value::as_str).unwrap_or("").to_string(),
            old_string: input.get("old_string").and_then(Value::as_str).unwrap_or("").to_string(),
            new_string: input.get("new_string").and_then(Value::as_str).unwrap_or("").to_string(),
        },
        _ => PendingDetail::Other,
    }
}

#[derive(Debug, Clone)]
pub struct PendingAction {
    pub tool_name: String,
    pub summary: String,
    pub detail: PendingDetail,
}
```

`execute_tool_call` 的 Confirm 分支:

```rust
            let action = PendingAction {
                tool_name: tool_name.to_string(),
                summary: describe_call(tool_name, &input),
                detail: detail_of(tool_name, &input),
            };
```

(全仓搜 `PendingAction {` 其余构造点一并补字段;`detail_of` 若测试外未用,加 `pub` 供 TUI 测试引用或保留私有 + 测试在同模块。)

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test --workspace`
Expected: 全部 PASS。

- [ ] **Step 5: Commit**

```bash
git add lex-core/src/security/mod.rs
git commit -m "feat(security): PendingAction 携带结构化 detail(TUI 弹层 diff 渲染数据源)"
```

---

### Task 2: ratatui 依赖 + theme.rs 增加 TUI 色板

**Files:**
- Modify: `lex-cli/Cargo.toml`(新增 ratatui)
- Modify: `lex-cli/src/ui/theme.rs`(追加 Color 常量,仍是唯一颜色定义处)

**Interfaces:**
- Produces: `theme::{C_ACCENT, C_ERROR, C_WARN, C_DIM, C_SUCCESS, C_THINKING, C_DIFF_ADD, C_DIFF_DEL, C_BORDER, C_BG_MODAL}: ratatui::style::Color`。消费方:Task 4/5/6 全部 TUI 渲染代码。

- [ ] **Step 1: 加依赖**

`lex-cli/Cargo.toml` `[dependencies]` 追加(与现有 `crossterm = "0.28"` 对齐):

```toml
ratatui = "0.28"
```

- [ ] **Step 2: theme.rs 追加常量**

`lex-cli/src/ui/theme.rs` 末尾追加:

```rust
// —— TUI(ratatui)色板:与上方 ANSI 主题同源的唯一定义处,TUI 渲染代码只允许从这里取色 ——
use ratatui::style::Color;
pub const C_ACCENT: Color = Color::Rgb(59, 130, 246); // 对应 ACCENT 蓝
pub const C_ERROR: Color = Color::Red;
pub const C_WARN: Color = Color::Yellow;
pub const C_SUCCESS: Color = Color::Green;
pub const C_DIM: Color = Color::DarkGray;
pub const C_THINKING: Color = Color::Magenta;
pub const C_DIFF_ADD: Color = Color::Green; // diff 增行
pub const C_DIFF_DEL: Color = Color::Red;   // diff 删行
pub const C_BORDER: Color = Color::DarkGray;
pub const C_BG_MODAL: Color = Color::Black; // 弹层底色,压住下层内容
```

- [ ] **Step 3: 编译验证**

Run: `cargo build -p lex-cli`
Expected: 编译通过。

- [ ] **Step 4: Commit**

```bash
git add lex-cli/Cargo.toml lex-cli/src/ui/theme.rs
git commit -m "feat(cli): 引入 ratatui 0.28 并在 theme.rs 落定 TUI 色板"
```

---

### Task 3: tui/diff.rs — 基础行级 diff(纯函数 + 单测)

**Files:**
- Create: `lex-cli/src/tui/mod.rs`(空壳 `pub mod diff;`,Task 5 继续扩充)
- Create: `lex-cli/src/tui/diff.rs`
- Test: `lex-cli/src/tui/diff.rs`(`mod tests`)

**Interfaces:**
- Produces:
  - `pub enum DiffLine { Add(String), Del(String), Ctx(String) }`
  - `pub fn line_diff(old: &str, new: &str) -> Vec<DiffLine>`(公共前/后缀裁剪,中段 old→Del、new→Add;最多输出 400 行,超出截断并在末尾追加 `Del("…(截断)")`)
- 语义说明:这是"最基础的行级 diff",不做 LCS/语法解析(任务书明确允许)。消费方:Task 5 确认弹层。

- [ ] **Step 1: 写失败测试**

`lex-cli/src/tui/diff.rs`:

```rust
/// 基础行级 diff:公共前缀/后缀保持,中段按"删-增"成对输出。
/// 不做 LCS 与语法解析(Phase 6 任务书:先做最基础的着色即可)。
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum DiffLine {
    Add(String),
    Del(String),
    Ctx(String),
}

const MAX_DIFF_LINES: usize = 400;

pub fn line_diff(old: &str, new: &str) -> Vec<DiffLine> {
    let old_lines: Vec<&str> = old.lines().collect();
    let new_lines: Vec<&str> = new.lines().collect();
    // 公共前缀
    let mut prefix = 0usize;
    while prefix < old_lines.len() && prefix < new_lines.len() && old_lines[prefix] == new_lines[prefix] {
        prefix += 1;
    }
    // 公共后缀(不越过前缀)
    let mut suffix = 0usize;
    while suffix < old_lines.len() - prefix
        && suffix < new_lines.len() - prefix
        && old_lines[old_lines.len() - 1 - suffix] == new_lines[new_lines.len() - 1 - suffix]
    {
        suffix += 1;
    }
    let old_mid = &old_lines[prefix..old_lines.len() - suffix];
    let new_mid = &new_lines[prefix..new_lines.len() - suffix];

    let mut out: Vec<DiffLine> = Vec::new();
    for l in &old_lines[..prefix] {
        out.push(DiffLine::Ctx((*l).to_string()));
    }
    for l in old_mid {
        out.push(DiffLine::Del((*l).to_string()));
    }
    for l in new_mid {
        out.push(DiffLine::Add((*l).to_string()));
    }
    for l in &old_lines[old_lines.len() - suffix..] {
        out.push(DiffLine::Ctx((*l).to_string()));
    }
    if out.len() > MAX_DIFF_LINES {
        out.truncate(MAX_DIFF_LINES);
        out.push(DiffLine::Del("…(diff 已截断)".into()));
    }
    out
}

#[cfg(test)]
mod tests {
    use super::*;

    fn kinds(v: &[DiffLine]) -> Vec<&str> {
        v.iter().map(|l| match l {
            DiffLine::Add(_) => "+",
            DiffLine::Del(_) => "-",
            DiffLine::Ctx(_) => " ",
        }).collect()
    }

    #[test]
    fn identical_input_is_all_context() {
        let d = line_diff("a\nb\n", "a\nb\n");
        assert_eq!(kinds(&d), vec![" ", " "]);
    }

    #[test]
    fn middle_replacement_yields_del_then_add() {
        let d = line_diff("fn a() {}\nfn old() {}\nfn c() {}", "fn a() {}\nfn new() {}\nfn c() {}");
        assert_eq!(kinds(&d), vec![" ", "-", "+", " "]);
        assert_eq!(d[1], DiffLine::Del("fn old() {}".into()));
        assert_eq!(d[2], DiffLine::Add("fn new() {}".into()));
    }

    #[test]
    fn pure_append_keeps_prefix_context() {
        let d = line_diff("a\nb", "a\nb\nc");
        assert_eq!(kinds(&d), vec![" ", " ", "+"]);
        assert_eq!(d[2], DiffLine::Add("c".into()));
    }

    #[test]
    fn empty_to_content_is_all_add() {
        let d = line_diff("", "x\ny");
        assert_eq!(kinds(&d), vec!["+", "+"]);
    }

    #[test]
    fn output_is_capped() {
        let old = String::new();
        let new = "x\n".repeat(600);
        let d = line_diff(&old, &new);
        assert_eq!(d.len(), MAX_DIFF_LINES + 1);
        assert_eq!(d.last(), Some(&DiffLine::Del("…(diff 已截断)".into())));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p lex-cli tui::diff`
Expected: FAIL,模块不存在。

- [ ] **Step 3: 实现**

按 Step 1 代码落盘;`lex-cli/src/tui/mod.rs` 建空壳:

```rust
pub mod diff;
```

`lex-cli/src/main.rs` 顶部 `mod confirm;` 旁加 `mod tui;`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p lex-cli tui::diff`
Expected: 5 个测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add lex-cli/src/tui lex-cli/src/main.rs
git commit -m "feat(tui): 基础行级 diff 纯函数(前/后缀裁剪 + 截断保护)"
```

---

### Task 4: UiEvent / UiCommand 事件模型 + TuiConfirm 权限处理器

**Files:**
- Create: `lex-cli/src/tui/event.rs`
- Create: `lex-cli/src/tui/confirm.rs`
- Modify: `lex-cli/src/tui/mod.rs`(挂模块)
- Test: `lex-cli/src/tui/confirm.rs`(`mod tests`)

**Interfaces:**
- Produces:
  - `pub enum UiEvent { TurnStarted, TextDelta(String), ThinkingDelta(String), ToolStart { name: String }, ToolResult { tool_name: String, first_line: String, is_error: bool }, Usage { input: u64, output: u64, cache_hit: u64 }, Confirm { action: PendingAction, responder: tokio::sync::oneshot::Sender<bool> }, TurnDone { ok: bool, message: String }, Exit }`
  - `pub enum UiCommand { Submit(String), Interrupt, Quit }`
  - `pub struct TuiConfirm { tx: std::sync::mpsc::Sender<UiEvent> }` + `pub fn new(tx) -> Self`;实现 `PermissionHandler`。
- 消费方:Task 5(AppState.apply)、Task 6(事件循环与 agent 侧接线)。

- [ ] **Step 1: 写失败测试**

`lex-cli/src/tui/confirm.rs`:

```rust
use anyhow::Result;
use async_trait::async_trait;
use lex_core::error::LexError;
use lex_core::security::{PendingAction, PermissionHandler};

use super::event::UiEvent;

/// TUI 模式的权限确认:把确认请求转发给 UI 线程弹层,经 oneshot 等待用户裁决。
pub struct TuiConfirm {
    tx: std::sync::mpsc::Sender<UiEvent>,
}

impl TuiConfirm {
    pub fn new(tx: std::sync::mpsc::Sender<UiEvent>) -> Self {
        TuiConfirm { tx }
    }
}

#[async_trait]
impl PermissionHandler for TuiConfirm {
    async fn confirm(&self, action: &PendingAction) -> Result<bool> {
        let (tx, rx) = tokio::sync::oneshot::channel();
        self.tx
            .send(UiEvent::Confirm { action: action.clone(), responder: tx })
            .map_err(|_| LexError::Io(std::io::Error::new(std::io::ErrorKind::BrokenPipe, "UI 已退出,无法确认")))?;
        // UI 线程退出等价于"拒绝":任何情况下不悬挂
        Ok(rx.await.unwrap_or(false))
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use lex_core::security::PendingDetail;

    fn action() -> PendingAction {
        PendingAction { tool_name: "bash_exec".into(), summary: "执行命令: ls".into(), detail: PendingDetail::Bash { command: "ls".into() } }
    }

    #[tokio::test]
    async fn confirm_round_trip_true() {
        let (tx, rx) = std::sync::mpsc::channel::<UiEvent>();
        let handler = TuiConfirm::new(tx);
        let h = tokio::spawn(async move { handler.confirm(&action()).await });
        match rx.recv().unwrap_or_else(|_| panic!("应收到 Confirm 事件")) {
            UiEvent::Confirm { action: a, responder } => responder.send(true).unwrap_or(()),
            o => panic!("期望 Confirm,实际 {o:?}"),
        }
        assert!(h.await.unwrap_or(false));
    }

    #[tokio::test]
    async fn dropped_ui_means_denied() {
        let (tx, rx) = std::sync::mpsc::channel::<UiEvent>();
        let handler = TuiConfirm::new(tx);
        drop(rx); // 模拟 UI 已退出
        assert!(handler.confirm(&action()).await.is_err(), "UI 退出应返回错误而非悬挂");
    }
}
```

注:`UiEvent` 含 `oneshot::Sender`(不可 `Debug`/`Clone`),枚举不加 derive。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p lex-cli tui::confirm`
Expected: FAIL,`UiEvent` 不存在。

- [ ] **Step 3: 实现 event.rs**

`lex-cli/src/tui/event.rs`:

```rust
use lex_core::security::PendingAction;

/// agent/工具侧 → UI 线程 的事件(经 std mpsc,非阻塞 send)
pub enum UiEvent {
    TurnStarted,
    TextDelta(String),
    ThinkingDelta(String),
    ToolStart { name: String },
    ToolResult { tool_name: String, first_line: String, is_error: bool },
    Usage { input: u64, output: u64, cache_hit: u64 },
    Confirm { action: PendingAction, responder: tokio::sync::oneshot::Sender<bool> },
    TurnDone { ok: bool, message: String },
    Exit,
}

/// UI 线程 → agent 侧 的命令(经 tokio mpsc,同步线程用 blocking_send)
pub enum UiCommand {
    Submit(String),
    Interrupt,
    Quit,
}
```

`lex-cli/src/tui/mod.rs`:

```rust
pub mod confirm;
pub mod diff;
pub mod event;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p lex-cli tui::confirm`
Expected: 2 个测试 PASS。

- [ ] **Step 5: Commit**

```bash
git add lex-cli/src/tui
git commit -m "feat(tui): UiEvent/UiCommand 事件模型与 TuiConfirm 权限处理器(oneshot 裁决回路)"
```

---

### Task 5: AppState + 布局/弹层渲染(TestBackend 可测)

**Files:**
- Create: `lex-cli/src/tui/state.rs`
- Create: `lex-cli/src/tui/draw.rs`
- Test: `lex-cli/src/tui/draw.rs`(`mod tests`,ratatui `TestBackend` 断言缓冲内容)

**Interfaces:**
- Consumes: `UiEvent`(Task 4)、`line_diff`(Task 3)、theme 色板(Task 2)、`lex_core::tools::Todo`。
- Produces(消费方:Task 6):
  - `pub struct AppState { .. }` + `AppState::new()`、`pub fn apply(&mut self, ev: UiEvent, todos: &std::sync::Mutex<Vec<lex_core::tools::Todo>>)`、`pub enum KeyOutcome { None, Submit(String), Confirm(bool), Interrupt, Quit }` + `pub fn handle_key(&mut self, key: crossterm::event::KeyEvent) -> KeyOutcome`、`pub fn push_line(&mut self, line: ratatui::text::Line<'static>)`(私有渲染辅助)
  - `pub fn draw(app: &mut AppState, f: &mut ratatui::Frame)`(主区 + 待办面板 + 输入盒 + 状态行 + 确认弹层)
  - 布局:`垂直 [Min(3) 主区, Length(3) 输入盒, Length(1) 状态行]`,主区水平 `[Percentage(75) 对话, Percentage(25) 待办]`;弹层居中 `80% × 60%`。

- [ ] **Step 1: 写失败测试(draw.rs)**

```rust
#[cfg(test)]
mod tests {
    use super::state::{AppState, KeyOutcome};
    use super::draw;
    use crate::tui::event::UiEvent;
    use crate::tui::diff::DiffLine;
    use lex_core::security::{PendingAction, PendingDetail};
    use ratatui::{backend::TestBackend, Terminal};
    use std::sync::{Arc, Mutex};

    fn render(app: &mut AppState, w: u16, h: u16) -> String {
        let backend = TestBackend::new(w, h);
        let mut term = Terminal::new(backend).unwrap_or_else(|e| panic!("{e}"));
        term.draw(|f| draw(app, f)).unwrap_or_else(|e| panic!("{e}"));
        let buf = term.backend().buffer();
        let mut s = String::new();
        for y in 0..buf.area.height {
            for x in 0..buf.area.width {
                s.push_str(buf[(x, y)].symbol());
            }
            s.push('\n');
        }
        s
    }

    #[test]
    fn layout_regions_present() {
        let todos = Arc::new(Mutex::new(vec![]));
        let mut app = AppState::new();
        app.apply(UiEvent::TextDelta("你好,世界".into()), &todos);
        let screen = render(&mut app, 80, 24);
        assert!(screen.contains("你好,世界"), "主输出区应渲染文本");
        assert!(screen.contains("待办"), "右侧应有待办面板标题");
        assert!(screen.contains("›"), "底部应有输入盒提示符");
    }

    #[test]
    fn todo_panel_reflects_shared_state() {
        let todos = Arc::new(Mutex::new(vec![
            lex_core::tools::Todo { content: "写测试".into(), status: lex_core::tools::TodoStatus::InProgress },
        ]));
        let mut app = AppState::new();
        app.apply(UiEvent::ToolResult { tool_name: "todo_write".into(), first_line: "待办已更新".into(), is_error: false }, &todos);
        let screen = render(&mut app, 80, 24);
        assert!(screen.contains("写测试"));
    }

    #[test]
    fn confirm_modal_renders_diff_colored() {
        let todos = Arc::new(Mutex::new(vec![]));
        let mut app = AppState::new();
        app.open_confirm(PendingAction {
            tool_name: "file_edit".into(),
            summary: "编辑文件: a.rs".into(),
            detail: PendingDetail::FileEdit { path: "a.rs".into(), old_string: "old".into(), new_string: "new".into() },
        });
        let screen = render(&mut app, 80, 24);
        assert!(screen.contains("确认操作"), "应有弹层标题");
        assert!(screen.contains("- old"), "弹层应渲染删除行");
        assert!(screen.contains("+ new"), "弹层应渲染新增行");
        assert!(screen.contains("[y] 允许"), "应展示快捷键");
    }

    #[test]
    fn keys_route_by_mode() {
        let todos = Arc::new(Mutex::new(vec![]));
        let mut app = AppState::new();
        // 输入模式:字符进输入盒,回车提交
        assert_eq!(app.handle_key(key_event(KeyCode::Char('h'))), KeyOutcome::None);
        assert_eq!(app.handle_key(key_event(KeyCode::Enter)), KeyOutcome::Submit("h".into()));
        // 确认模式:y/n 直接裁决
        app.open_confirm(PendingAction { tool_name: "bash_exec".into(), summary: "s".into(), detail: PendingDetail::Other });
        assert_eq!(app.handle_key(key_event(KeyCode::Char('y'))), KeyOutcome::Confirm(true));
        assert!(app.pending_confirm.is_none());
        app.open_confirm(PendingAction { tool_name: "bash_exec".into(), summary: "s".into(), detail: PendingDetail::Other });
        assert_eq!(app.handle_key(key_event(KeyCode::Esc)), KeyOutcome::Confirm(false));
    }

    // 测试辅助:构造 Press 键事件
    fn key_event(code: crossterm::event::KeyCode) -> crossterm::event::KeyEvent {
        crossterm::event::KeyEvent::new(code, crossterm::event::KeyModifiers::empty())
    }

    #[test]
    fn diff_lines_render_with_colors() {
        // 直接验证弹层 diff 行样式:增行绿、删行红(样式挂在 Span 上)
        let lines = vec![DiffLine::Add("new".into()), DiffLine::Del("old".into())];
        let styled = super::styled_diff_lines(&lines);
        assert_eq!(styled[0].spans[0].style.fg, Some(crate::ui::theme::C_DIFF_ADD));
        assert_eq!(styled[1].spans[0].style.fg, Some(crate::ui::theme::C_DIFF_DEL));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p lex-cli tui::draw`
Expected: FAIL,`state`/`draw` 模块不存在。

- [ ] **Step 3: 实现 state.rs**

`lex-cli/src/tui/state.rs`:

```rust
use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
use lex_core::security::PendingAction;
use ratatui::text::Line;

use super::event::UiEvent;

pub struct ConfirmModal {
    pub action: PendingAction,
    pub responder: tokio::sync::oneshot::Sender<bool>,
}

pub struct AppState {
    pub transcript: Vec<Line<'static>>,
    pub todos: Vec<lex_core::tools::Todo>,
    pub input: String,
    pub status: String,
    pub busy: bool,
    pub usage: Option<String>,
    pub pending_confirm: Option<ConfirmModal>,
    pub quit: bool,
}

impl AppState {
    pub fn new() -> Self {
        AppState {
            transcript: Vec::new(),
            todos: Vec::new(),
            input: String::new(),
            status: "就绪".into(),
            busy: false,
            usage: None,
            pending_confirm: None,
            quit: false,
        }
    }

    /// 事件应用:全部渲染状态变更只发生在 UI 线程
    pub fn apply(&mut self, ev: UiEvent, todos: &std::sync::Mutex<Vec<lex_core::tools::Todo>>) {
        match ev {
            UiEvent::TurnStarted => {
                self.busy = true;
                self.status = "思考中…".into();
                self.usage = None;
            }
            UiEvent::TextDelta(t) => {
                // 简单换行切分;流式粒度的样式适配沿用纯文本模式的 markdown 模块即可按需扩展
                for (i, seg) in t.split('\n').enumerate() {
                    if i > 0 {
                        self.transcript.push(Line::from("".to_string()));
                    }
                    if !seg.is_empty() {
                        self.transcript.push(Line::from(seg.to_string()));
                    }
                }
            }
            UiEvent::ThinkingDelta(_) => {
                self.status = "思考中…".into();
            }
            UiEvent::ToolStart { name } => {
                self.status = format!("执行 {name}…");
            }
            UiEvent::ToolResult { tool_name, first_line, is_error } => {
                let mark = if is_error { "⎿ ✗ " } else { "⎿ " };
                self.transcript.push(Line::from(format!("  {mark}{first_line}")));
                // 待办面板:todo_write 结果到达即从共享状态重读
                if tool_name == "todo_write" {
                    self.todos = todos.lock().unwrap_or_else(|p| p.into_inner()).clone();
                }
            }
            UiEvent::Usage { input, output, cache_hit } => {
                self.usage = Some(if cache_hit > 0 {
                    format!("输入 {input} · 输出 {output} · 缓存命中 {cache_hit}")
                } else {
                    format!("输入 {input} · 输出 {output}")
                });
            }
            UiEvent::Confirm { action, responder } => {
                self.pending_confirm = Some(ConfirmModal { action, responder });
            }
            UiEvent::TurnDone { ok, message } => {
                self.busy = false;
                if ok {
                    self.status = "就绪".into();
                } else {
                    self.status = "本轮已中断/失败".into();
                    if !message.is_empty() {
                        self.transcript.push(Line::from(format!("⚠ {message}")));
                    }
                }
            }
            UiEvent::Exit => {
                self.quit = true;
            }
        }
    }

    pub fn open_confirm(&mut self, action: PendingAction) {
        // 测试辅助:直接构造无 responder 的弹层
        let (tx, _rx) = tokio::sync::oneshot::channel();
        self.pending_confirm = Some(ConfirmModal { action, responder: tx });
    }

    fn answer_confirm(&mut self, allow: bool) {
        if let Some(modal) = self.pending_confirm.take() {
            let _ = modal.responder.send(allow);
        }
    }

    pub fn handle_key(&mut self, key: KeyEvent) -> KeyOutcome {
        // 确认弹层优先:y/Enter 允许,n/Esc/其他 拒绝
        if self.pending_confirm.is_some() {
            return match key.code {
                KeyCode::Char('y') | KeyCode::Char('Y') => {
                    self.answer_confirm(true);
                    KeyOutcome::Confirm(true)
                }
                KeyCode::Char('n') | KeyCode::Char('N') | KeyCode::Esc => {
                    self.answer_confirm(false);
                    KeyOutcome::Confirm(false)
                }
                _ => KeyOutcome::None,
            };
        }
        match (key.code, key.modifiers) {
            (KeyCode::Char('c'), KeyModifiers::CONTROL) => {
                if self.busy {
                    KeyOutcome::Interrupt
                } else {
                    self.quit = true;
                    KeyOutcome::Quit
                }
            }
            (KeyCode::Enter, _) if !self.input.trim().is_empty() => {
                let text = std::mem::take(&mut self.input);
                KeyOutcome::Submit(text)
            }
            (KeyCode::Backspace, _) => {
                self.input.pop();
                KeyOutcome::None
            }
            (KeyCode::Char('u'), KeyModifiers::CONTROL) => {
                self.input.clear();
                KeyOutcome::None
            }
            (KeyCode::Char(c), m) if !m.contains(KeyModifiers::CONTROL) => {
                self.input.push(c);
                KeyOutcome::None
            }
            _ => KeyOutcome::None,
        }
    }
}

impl Default for AppState {
    fn default() -> Self {
        Self::new()
    }
}

pub enum KeyOutcome {
    None,
    Submit(String),
    Confirm(bool),
    Interrupt,
    Quit,
}
```

- [ ] **Step 4: 实现 draw.rs**

`lex-cli/src/tui/draw.rs`:

```rust
use crate::tui::diff::{line_diff, DiffLine};
use crate::tui::state::AppState;
use crate::ui::theme;
use ratatui::layout::{Constraint, Layout, Rect};
use ratatui::style::Style;
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, Borders, Clear, List, ListItem, Paragraph};
use ratatui::Frame;

pub fn draw(app: &mut AppState, f: &mut Frame) {
    let chunks = Layout::vertical([Constraint::Min(3), Constraint::Length(3), Constraint::Length(1)]).split(f.area());
    let cols = Layout::horizontal([Constraint::Percentage(75), Constraint::Percentage(25)]).split(chunks[0]);

    draw_transcript(f, cols[0], app);
    draw_todos(f, cols[1], app);
    draw_input(f, chunks[1], app);
    draw_status(f, chunks[2], app);
    if app.pending_confirm.is_some() {
        draw_confirm(f, app);
    }
}

fn draw_transcript(f: &mut Frame, area: Rect, app: &mut AppState) {
    let block = Block::default()
        .borders(Borders::ALL)
        .border_style(Style::default().fg(theme::C_BORDER))
        .title(" 对话 ");
    let inner = block.inner(area);
    f.render_widget(block, area);
    let visible = inner.height as usize;
    let start = app.transcript.len().saturating_sub(visible.saturating_sub(1));
    let lines: Vec<Line> = app.transcript[start..].to_vec();
    f.render_widget(Paragraph::new(lines), inner);
}

fn draw_todos(f: &mut Frame, area: Rect, app: &mut AppState) {
    let block = Block::default()
        .borders(Borders::ALL)
        .border_style(Style::default().fg(theme::C_BORDER))
        .title(" 待办 ");
    let inner = block.inner(area);
    let items: Vec<ListItem> = app
        .todos
        .iter()
        .map(|t| {
            let (mark, style) = match t.status {
                lex_core::tools::TodoStatus::Pending => ("☐", Style::default().fg(theme::C_DIM)),
                lex_core::tools::TodoStatus::InProgress => ("◐", Style::default().fg(theme::C_ACCENT)),
                lex_core::tools::TodoStatus::Completed => ("☑", Style::default().fg(theme::C_SUCCESS)),
            };
            ListItem::new(Line::from(Span::styled(format!("{mark} {}", t.content), style)))
        })
        .collect();
    f.render_widget(block, area);
    f.render_widget(List::new(items), inner);
}

fn draw_input(f: &mut Frame, area: Rect, app: &mut AppState) {
    let block = Block::default()
        .borders(Borders::ALL)
        .border_style(Style::default().fg(if app.busy { theme::C_BORDER } else { theme::C_ACCENT }))
        .title(" 输入(Enter 提交,Ctrl+C 中断/退出) ");
    let inner = block.inner(area);
    f.render_widget(block, area);
    f.render_widget(Paragraph::new(format!("› {}", app.input)), inner);
}

fn draw_status(f: &mut Frame, area: Rect, app: &mut AppState) {
    let right = app.usage.clone().unwrap_or_default();
    let left = app.status.clone();
    f.render_widget(Paragraph::new(Line::from(vec![
        Span::styled(left, Style::default().fg(theme::C_DIM)),
        Span::raw("  "),
        Span::styled(right, Style::default().fg(theme::C_DIM)),
    ])), area);
}

/// diff 行 → 带色 Span 行(增绿删红,公开供测试断言)
pub fn styled_diff_lines(diff: &[DiffLine]) -> Vec<Line<'static>> {
    diff.iter()
        .map(|l| match l {
            DiffLine::Add(s) => Line::from(Span::styled(format!("+ {s}"), Style::default().fg(theme::C_DIFF_ADD))),
            DiffLine::Del(s) => Line::from(Span::styled(format!("- {s}"), Style::default().fg(theme::C_DIFF_DEL))),
            DiffLine::Ctx(s) => Line::from(Span::styled(format!("  {s}"), Style::default().fg(theme::C_DIM))),
        })
        .collect()
}

fn draw_confirm(f: &mut Frame, app: &mut AppState) {
    let Some(modal) = app.pending_confirm.as_ref() else { return };
    let area = centered_rect(80, 60, f.area());
    f.render_widget(Clear, area);
    let block = Block::default()
        .borders(Borders::ALL)
        .border_style(Style::default().fg(theme::C_WARN))
        .title(format!(" 确认操作 [{}] ", modal.action.tool_name));
    let inner = block.inner(area);
    f.render_widget(block, area);

    let mut lines: Vec<Line> = vec![Line::from(Span::styled(modal.action.summary.clone(), Style::default().fg(theme::C_WARN)))];
    lines.push(Line::from(""));
    match &modal.action.detail {
        lex_core::security::PendingDetail::Bash { command } => {
            lines.push(Line::from(Span::styled(format!("$ {command}"), Style::default().fg(theme::C_ACCENT))));
        }
        lex_core::security::PendingDetail::FileEdit { path, old_string, new_string } => {
            lines.push(Line::from(Span::styled(format!("文件: {path}"), Style::default().fg(theme::C_ACCENT))));
            lines.extend(styled_diff_lines(&line_diff(old_string, new_string)));
        }
        lex_core::security::PendingDetail::Other => {}
    }
    lines.push(Line::from(""));
    lines.push(Line::from(Span::styled("[y] 允许   [n/Esc] 拒绝", Style::default().fg(theme::C_DIM))));
    f.render_widget(Paragraph::new(lines), inner);
}

fn centered_rect(percent_x: u16, percent_y: u16, outer: Rect) -> Rect {
    let v = Layout::vertical([
        Constraint::Percentage((100 - percent_y) / 2),
        Constraint::Percentage(percent_y),
        Constraint::Percentage((100 - percent_y) / 2),
    ])
    .split(outer);
    Layout::horizontal([
        Constraint::Percentage((100 - percent_x) / 2),
        Constraint::Percentage(percent_x),
        Constraint::Percentage((100 - percent_x) / 2),
    ])
    .split(v[1])[1]
}
```

注:任务书要求"命令原文或文件 diff,而不是笼统提示"——`summary` 行只是补充,弹层主体是 `$ 命令原文` 或带色 diff。`Stylize` import 若未用到可删。

- [ ] **Step 5: 跑测试确认通过**

Run: `cargo test -p lex-cli`
Expected: 全部 PASS(含 markdown/editor 既有测试)。

- [ ] **Step 6: Commit**

```bash
git add lex-cli/src/tui lex-cli/src/main.rs
git commit -m "feat(tui): AppState 事件应用 + 多区域布局与确认弹层(TestBackend 断言,diff 增删行着色)"
```

---

### Task 6: 事件循环与 agent 接线(渲染线程 ↔ tokio agent,Ctrl+C 打断,退出恢复终端)

**Files:**
- Create: `lex-cli/src/tui/run.rs`
- Modify: `lex-cli/src/tui/mod.rs`(挂模块)
- Test: `lex-cli/src/tui/run.rs`(`mod tests`:纯函数级;循环本体靠 Task 7 手工冒烟)

**Interfaces:**
- Consumes: `AgentLoop`(字段全部 pub)、`TuiConfirm`/`UiEvent`/`UiCommand`(Task 4)、`AppState::apply`/`handle_key`/`draw`(Task 5)、`todos: Arc<Mutex<Vec<Todo>>>`(subagent 计划 Task 7 已提升到 `run()`)。
- Produces: `pub async fn run_tui(agent: lex_core::agent::AgentLoop, todos: std::sync::Arc<std::sync::Mutex<Vec<lex_core::tools::Todo>>>, cwd: &std::path::Path) -> anyhow::Result<()>`

- [ ] **Step 1: 实现 run.rs**

```rust
use anyhow::{Context, Result};
use crossterm::event::{self, Event, KeyEventKind};
use lex_core::agent::AgentLoop;
use lex_core::provider::ProviderEvent;
use lex_core::tools::Todo;
use std::sync::{Arc, Mutex};
use std::time::Duration;

use super::confirm::TuiConfirm;
use super::draw::draw;
use super::event::{UiCommand, UiEvent};
use super::state::{AppState, KeyOutcome};

/// TUI 主入口:agent 留在 tokio 侧,渲染跑独立线程,双 channel 解耦。
pub async fn run_tui(
    mut agent: AgentLoop,
    todos: Arc<Mutex<Vec<Todo>>>,
    cwd: &std::path::Path,
) -> Result<()> {
    let (ev_tx, ev_rx) = std::sync::mpsc::channel::<UiEvent>();
    // UI → agent:普通命令与打断分两条通道(run_turn 期间用 select! 监听打断)
    let (cmd_tx, mut cmd_rx) = tokio::sync::mpsc::channel::<UiCommand>(16);
    let (irq_tx, mut irq_rx) = tokio::sync::mpsc::channel::<UiCommand>(4);

    // 工具结果回调 → UI(⎿ 行 + 待办面板刷新)
    let hook_tx = ev_tx.clone();
    agent.on_tool_result = Some(Arc::new(move |info| {
        let _ = hook_tx.send(UiEvent::ToolResult { tool_name: info.tool_name.clone(), first_line: info.first_line.clone(), is_error: info.is_error });
    }));
    agent.handler = Box::new(TuiConfirm::new(ev_tx.clone()));

    let project = crate::detect_project_type(cwd);
    let ui_thread = std::thread::spawn(move || ui_loop(ev_rx, cmd_tx, irq_tx, todos, project));

    // —— agent 侧循环(tokio)——
    loop {
        let Some(cmd) = cmd_rx.recv().await else { break };
        match cmd {
            UiCommand::Submit(text) => {
                let _ = ev_tx.send(UiEvent::TurnStarted);
                let ev = ev_tx.clone();
                let mut on_event = move |e: &ProviderEvent| {
                    let _ = forward_event(e, &ev);
                };
                let result = tokio::select! {
                    res = agent.run_turn(&text, &mut on_event) => res,
                    Some(UiCommand::Interrupt) = irq_rx.recv() => {
                        agent.recover_interrupt();
                        let _ = ev_tx.send(UiEvent::TurnDone { ok: false, message: "已中断本轮任务,可继续输入".into() });
                        continue;
                    }
                };
                match result {
                    Ok(_) => {
                        // 清掉可能残留的迟到打断信号,避免下一轮被误中断
                        while irq_rx.try_recv().is_ok() {}
                        let _ = ev_tx.send(UiEvent::TurnDone { ok: true, message: String::new() });
                    }
                    Err(e) => {
                        while irq_rx.try_recv().is_ok() {}
                        let _ = ev_tx.send(UiEvent::TurnDone { ok: false, message: format!("本轮失败: {e:#}") });
                    }
                }
            }
            UiCommand::Quit => break,
            UiCommand::Interrupt => {} // 空闲期打断无意义,忽略
        }
    }
    let _ = ev_tx.send(UiEvent::Exit);
    let _ = ui_thread.join();
    Ok(())
}

fn forward_event(e: &ProviderEvent, tx: &std::sync::mpsc::Sender<UiEvent>) -> Result<()> {
    match e {
        ProviderEvent::TextDelta(t) => tx.send(UiEvent::TextDelta(t.clone())).map_err(mpsc_closed)?,
        ProviderEvent::ThinkingDelta(t) => tx.send(UiEvent::ThinkingDelta(t.clone())).map_err(mpsc_closed)?,
        ProviderEvent::ToolUseStart { name, .. } => tx.send(UiEvent::ToolStart { name: name.clone() }).map_err(mpsc_closed)?,
        // ToolUseComplete 不另发事件:执行中状态由 ToolStart 维持,结果由 ⎿ 回调呈现
        ProviderEvent::ToolUseComplete { .. } => {}
        ProviderEvent::Completed { usage } => {
            tx.send(UiEvent::Usage { input: usage.input_tokens, output: usage.output_tokens, cache_hit: usage.cache_hit_tokens }).map_err(mpsc_closed)?
        }
        ProviderEvent::ToolUseDelta { .. } => {}
    }
    Ok(())
}

fn mpsc_closed(_: std::sync::mpsc::SendError<UiEvent>) -> anyhow::Error {
    anyhow::anyhow!("UI 线程已退出")
}

/// 渲染线程主体:约 50Hz 排空事件 + 处理按键 + 重绘
fn ui_loop(
    ev_rx: std::sync::mpsc::Receiver<UiEvent>,
    cmd_tx: tokio::sync::mpsc::Sender<UiCommand>,
    irq_tx: tokio::sync::mpsc::Sender<UiCommand>,
    todos: Arc<Mutex<Vec<Todo>>>,
    project: String,
) {
    let mut terminal = match setup_terminal() {
        Ok(t) => t,
        Err(_) => return, // 终端初始化失败(如重定向):直接退出,由 agent 侧 Quit 收尾
    };
    let mut app = AppState::new();
    app.status = format!("就绪 · {project}");

    loop {
        // 1. 按键(非阻塞)
        let has_event = event::poll(Duration::from_millis(20)).unwrap_or(false);
        if has_event {
            if let Ok(ev) = event::read() {
                if let Event::Key(key) = ev {
                    if key.kind == KeyEventKind::Press {
                        match app.handle_key(key) {
                            KeyOutcome::None => {}
                            KeyOutcome::Submit(text) => {
                                let _ = cmd_tx.blocking_send(UiCommand::Submit(text));
                            }
                            KeyOutcome::Confirm(_) => {}
                            KeyOutcome::Interrupt => {
                                let _ = irq_tx.blocking_send(UiCommand::Interrupt);
                            }
                            KeyOutcome::Quit => {
                                let _ = cmd_tx.blocking_send(UiCommand::Quit);
                                break;
                            }
                        }
                    }
                }
            }
        }
        // 2. 排空 agent 事件
        while let Ok(ev) = ev_rx.try_recv() {
            app.apply(ev, &todos);
            if app.quit {
                break;
            }
        }
        if app.quit {
            break;
        }
        // 3. 重绘
        if terminal.draw(|f| draw(&mut app, f)).is_err() {
            break;
        }
    }
    let _ = restore_terminal(&mut terminal);
}

type TuiTerminal = ratatui::Terminal<ratatui::backend::CrosstermBackend<std::io::Stdout>>;

fn setup_terminal() -> Result<TuiTerminal> {
    crossterm::terminal::enable_raw_mode().context("启用 raw mode 失败")?;
    let mut stdout = std::io::stdout();
    crossterm::execute!(stdout, crossterm::terminal::EnterAlternateScreen).context("进入备用屏幕失败")?;
    let backend = ratatui::backend::CrosstermBackend::new(stdout);
    ratatui::Terminal::new(backend).context("初始化 ratatui 终端失败")
}

fn restore_terminal(terminal: &mut TuiTerminal) -> Result<()> {
    crossterm::execute!(terminal.backend_mut(), crossterm::terminal::LeaveAlternateScreen)
        .context("退出备用屏幕失败")?;
    crossterm::terminal::disable_raw_mode().context("关闭 raw mode 失败")?;
    Ok(())
}
```

注:`detect_project_type` 目前是 `main.rs` 私有函数,在本任务把它改为 `pub(crate) fn`(签名不变),TUI 状态行复用。**Ctrl+C 在 raw mode 下不会产生 SIGINT**(终端进入 raw 后由我们读按键),与纯文本路径的 `tokio::signal::ctrl_c()` 不冲突。

- [ ] **Step 2: 编译 + 全量测试**

Run: `cargo build --workspace && cargo test --workspace`
Expected: 编译通过,测试全 PASS。

- [ ] **Step 3: Commit**

```bash
git add lex-cli/src/tui lex-cli/src/main.rs
git commit -m "feat(tui): 事件循环接线(agent↔渲染线程双通道,Ctrl+C 打断走 select,终端恢复兜底)"
```

---

### Task 7: main.rs 接入 `--plain` 与 TTY 自动降级

**Files:**
- Modify: `lex-cli/src/main.rs`(Cli 参数、模式分发)
- Test: `lex-cli/src/main.rs`(纯函数单测)

**Interfaces:**
- Consumes: Task 6 `run_tui`。
- Produces: `fn use_tui(forced_plain: bool, stdout_tty: bool) -> bool`;`--plain` CLI 参数。

- [ ] **Step 1: 写失败测试**

`main.rs` 底部追加:

```rust
#[cfg(test)]
mod mode_tests {
    use super::use_tui;

    #[test]
    fn tui_requires_tty_and_absence_of_plain_flag() {
        assert!(use_tui(false, true));
        assert!(!use_tui(false, false), "非 TTY 必须降级纯文本");
        assert!(!use_tui(true, true), "--plain 强制纯文本");
        assert!(!use_tui(true, false));
    }
}
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p lex-cli mode_tests`
Expected: FAIL,`use_tui` 不存在。

- [ ] **Step 3: 实现模式分发**

`Cli` 结构体加参数:

```rust
    /// 强制纯文本流模式(跳过 TUI)
    #[arg(long)]
    plain: bool,
```

`run()` 内交互分支改造:

```rust
fn use_tui(forced_plain: bool, stdout_tty: bool) -> bool {
    !forced_plain && stdout_tty
}
```

```rust
    if cli.task.is_empty() {
        // cli.plain 强制纯文本;stdout 非 TTY(重定向/管道)自动降级
        if use_tui(cli.plain, std::io::stdout().is_tty()) {
            // TUI 模式:agent 与 todos 的所有权移交 run_tui(同一个 todos Arc 已注入 ToolContext)
            tui::run::run_tui(agent, todos, &cwd).await?;
        } else {
            interactive_session(&mut agent, &input, &cfg, &cwd).await?;
        }
    } else {
        // 单任务模式:保持纯文本流(输出可能被重定向,TUI 无意义)
        ...现有逻辑不变...
    }
```

配套小重构(接 subagent 计划 Task 7 的 `run()` 现状):`run()` 层创建 `let todos: Arc<Mutex<Vec<Todo>>> = Arc::new(Mutex::new(Vec::new()));` 后照旧传入 `build_loop`(该计划已完成),`run()` 作用域保留 `todos` 本体供 TUI 分支 move;`input` 仍传给 `interactive_session`(纯文本确认)。`is_tty()` 用现有 `use crossterm::tty::IsTty;` 导入(对 `Stdout` 可用),无需新依赖。

- [ ] **Step 4: 验证**

Run: `cargo test -p lex-cli && cargo build --release -p lex-cli`
Expected: 测试 PASS;release 产物生成于 `D:/lexcode-target/release/lex-code.exe`。

- [ ] **Step 5: Commit**

```bash
git add lex-cli/src/main.rs
git commit -m "feat(cli): --plain 参数与非 TTY 自动降级;交互模式接入 TUI"
```

---

### Task 8: 手工验收 + 文档

**Files:**
- Modify: `README.md`(使用文档:TUI 布局、`--plain`、快捷键)、`AGENTS.md`(UI 模块清单补 `tui/`,硬约束补"TUI 颜色同样只取 theme.rs")、`examples/smoke/README.md`(第 11 节)

**Interfaces:** 无代码接口;收尾文档。

- [ ] **Step 1: 手工验收清单(交互终端执行 release 产物)**

1. 交互模式启动:出现多区域布局(对话/待办/输入/状态行),无乱码。
2. 输入一个会触发 `todo_write` 的任务("用待办清单跟踪:1 读 README 2 总结"),待办面板随进度更新(☐/◐/☑)。
3. 触发 `bash_exec`:确认弹层显示**命令原文**;触发 `file_edit`:弹层显示带色 diff(`- old` / `+ new`);`y` 放行 / `n` 拒绝均正常回到对话流。
4. 任务执行中按 Ctrl+C:本轮中断、状态行提示、可继续输入(验证 `recover_interrupt` 后续轮正常)。
5. 空闲时 Ctrl+C:整屏恢复、无控制字符残留。
6. `lex-code --plain`:回到现有纯文本流模式,行为与 Phase 5 一致。
7. `lex-code | cat`(单任务重定向):纯文本输出,无 ANSI 残留(`cargo run -- task "…"` 也验一次)。

- [ ] **Step 2: 文档更新**

- `README.md` 新增"TUI 模式"小节:默认交互终端启用;`--plain` 强制纯文本;非 TTY 自动降级;快捷键表(Enter 提交 / Ctrl+C 中断或退出 / Ctrl+U 清空输入 / 弹层 y/n)。
- `AGENTS.md`:`lex-cli/src/` 清单补 `tui/(diff/event/state/draw/confirm/run)`;硬约束区补一行"确认弹层必须展示命令原文或 diff,禁止'是否继续?'式笼统提示"。
- `examples/smoke/README.md` 追加"第 11 节:TUI 冒烟"(照 Step 1 清单)。

- [ ] **Step 3: 全量回归 + Commit**

Run: `cargo test --workspace`
Expected: 全部 PASS。

```bash
git add -A
git commit -m "docs: Phase6 TUI 使用文档与冒烟步骤"
```

---

## 验收对照(任务书)

| 任务书要求 | 落点 |
|---|---|
| ratatui + crossterm 多区域布局 | Task 5(对话主区/待办面板/输入盒/状态行) |
| 待办面板实时反映 todo_write | Task 5(`apply` 里 ToolResult==todo_write 时从共享 Arc 重读) |
| 权限确认弹层展示命令原文/diff,支持快捷键 | Task 1(PendingDetail)+ Task 3(diff)+ Task 5(弹层与 y/n) |
| 渲染与 agent 事件流解耦,channel 送入,渲染不阻塞 agent | Task 4(事件模型)+ Task 6(std mpsc + 独立线程 + tokio select) |
| `--plain` 保留,非 TTY 自动降级 | Task 7(`use_tui` + 分发;单任务模式恒为纯文本) |
| 代码块/diff 基础 ANSI 着色,不上语法解析器 | Task 3 + Task 5(diff 增删行双色) |
| 重定向无乱码/控制字符残留 | Task 7(非 TTY 判定)+ Task 8 验收第 7 项 |
